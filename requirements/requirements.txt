# PoloOS — Python / YOLO pipeline dependencies
# Install into the project venv:  pip install -r requirements/requirements.txt
#
# Note: PyTorch / torchvision are NOT listed here because the correct wheel
# depends on the target platform (CUDA, ROCm, CPU-only, Pi ARM).
# Install manually after activating the venv:
#   macOS M-series: pip install torch torchvision
#   Pi 5 (aarch64): pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

ultralytics>=8.0
opencv-python>=4.8
numpy>=1.24
